{
  "rules": {
    ".write": "auth.uid === 'BDypN09tyPQ38XK1anDYfhTZFY53'",    
    ".read": true,
    "posts": {
      "$id": {
        "author": {
          ".validate": "newData.isString()"
        },
        "body": {
          ".validate": "newData.isString()"
        },
        "title": {
          ".validate": "newData.isString()"
        },
        "img": {
          ".validate": "newData.isString()"
        },
        "created": {
          // ".validate": "newData.val() < now"
        }
      }
    },
    "settings": {
      ".validate": true
    },
    "pages": {
      "$id": {
        "name": {
          ".validate": "newData.isString()"
        },
        "fields": {
          ".validate": true
        },
        "$other": {
          ".validate": false
        }
      }
    },
    "users": {
      "$uid": {
        ".write": "auth != null && auth.uid == $uid",
        "firstname": {
          ".validate": "newData.isString()"
        },
        "lastname": {
          ".validate": "newData.isString()"
        },
        "category": {
          ".validate": "newData.isString()"
        },
        "role": {
          ".validate": "newData.isString()"
        },
        "email": {
          ".validate": "newData.isString()"
        },
        "created": {
         ".validate": "newData.val() < now"
        }
      }
    },
    "media": {
      "$id": {
        // ".write": "auth != null && auth.uid == $uid && root.child('users').child(auth.uid).child('role').val() === 'admin' "
      }
    },
    "$other": {
      ".validate": false
    }
  }
}